language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-angularfire-fab' ]; then cd .. && eval "mv $currentfolder generator-angularfire-fab" && cd generator-angularfire-fab; fi

